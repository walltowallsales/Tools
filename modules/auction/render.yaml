services:
  - type: web
    name: sellerchamp-auction-inventory
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SELLERCHAMP_API_TOKEN
        sync: false
      - key: APP_PIN
        sync: false
