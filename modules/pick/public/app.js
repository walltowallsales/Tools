const $=s=>document.querySelector(s);
const PIN_TTL_MS=30*24*60*60*1000;
let PIN='';
try{
  const saved=JSON.parse(localStorage.getItem('pickPinAuth')||'null');
  if(saved&&saved.pin&&saved.expiresAt>Date.now()) PIN=saved.pin;
  else localStorage.removeItem('pickPinAuth');
}catch(_){localStorage.removeItem('pickPinAuth')}
let currentBatch=null,currentLines=[],idx=0;
async function api(url,opts={}){opts.headers={...(opts.headers||{}),'Content-Type':'application/json'};if(PIN)opts.headers['x-app-pin']=PIN;const r=await fetch(url,opts);const j=await r.json().catch(()=>({}));if(r.status===401){PIN='';localStorage.removeItem('pickPinAuth');showPin();throw new Error('PIN required')}if(!r.ok)throw new Error(j.error||`Error ${r.status}`);return j}
function show(id){['homeView','freightView','batchView','pickerView','pinView'].forEach(x=>$('#'+x).classList.toggle('hidden',x!==id));$('#homeBtn').classList.toggle('hidden',id==='homeView'||id==='pinView')}
function showPin(){$('#pinView').classList.remove('hidden');['homeView','freightView','batchView','pickerView'].forEach(x=>$('#'+x).classList.add('hidden'))}
$('#pinBtn').onclick=()=>{PIN=$('#pinInput').value.trim();loadHome().then(()=>{localStorage.setItem('pickPinAuth',JSON.stringify({pin:PIN,expiresAt:Date.now()+PIN_TTL_MS}));$('#pinErr').textContent=''}).catch(e=>{localStorage.removeItem('pickPinAuth');$('#pinErr').textContent=e.message})};
$('#homeBtn').onclick=()=>loadHome();
function fmtDate(s){return new Date(s).toLocaleString(undefined,{month:'short',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit'})}
async function loadHome(){show('homeView');$('#homeError').textContent='';await Promise.all([loadBatches(),loadPreview(),loadFreightBadge()])}
async function loadFreightBadge(){const r=await api('/api/freight');$('#freightBadge').textContent=r.outstandingCount;$('#freightQueueBtn').classList.toggle('hasOverdue',r.overdueCount>0);$('#freightQueueBtn').title=r.overdueCount?`${r.overdueCount} overdue freight item${r.overdueCount===1?'':'s'}`:''}
async function loadPreview(){const el=$('#preview');$('#createBtn').disabled=true;el.textContent='Checking SellerChamp…';try{const p=await api('/api/preview');el.innerHTML=`<strong>${p.newOrders}</strong> new orders • <strong>${p.totalUnits}</strong> units${p.excludedAlreadyBatched?` • ${p.excludedAlreadyBatched} already in saved batches`:''}`;$('#createBtn').disabled=p.newOrders<1}catch(e){el.textContent='Could not load SellerChamp';$('#homeError').textContent=e.message}}
async function loadBatches(){const {batches}=await api('/api/batches');const active=batches.filter(b=>b.status!=='archived'&&b.status!=='deleted'),archived=batches.filter(b=>b.status==='archived'),deleted=batches.filter(b=>b.status==='deleted');const el=$('#batchList'),ar=$('#archiveList'),dr=$('#deletedList');el.innerHTML=active.length?'':'<div class="card">No active pick batches.</div>';ar.innerHTML=archived.length?'':'<div class="card">No archived pick batches.</div>';dr.innerHTML=deleted.length?'':'<div class="card">No deleted pick batches.</div>';$('#archiveCount').textContent=`(${archived.length})`;$('#deletedCount').textContent=`(${deleted.length})`;for(const b of active)appendBatchCard(el,b);for(const b of archived)appendBatchCard(ar,b);for(const b of deleted)appendDeletedCard(dr,b)}
function appendBatchCard(el,b){const d=document.createElement('div');d.className='batch';d.innerHTML=`<div><h3>${esc(b.name)}</h3><div class="meta">${fmtDate(b.createdAt)} • ${b.orderCount} orders • ${b.uniqueStops} pick stops • ${b.totalUnits} units</div><div class="meta">${b.pickedStops}/${b.uniqueStops} stops picked</div></div><div class="badge">${b.status.replace('_',' ')}</div>`;d.onclick=()=>openBatch(b.id);el.appendChild(d)}
$('#freightQueueBtn').onclick=()=>loadFreightQueue();
$('#freightRefreshBtn').onclick=()=>loadFreightQueue();
async function loadFreightQueue(){show('freightView');const list=$('#freightList'),history=$('#freightHistory');list.innerHTML='<div class="card">Loading freight…</div>';history.innerHTML='';try{const r=await api('/api/freight'),active=r.freight.filter(f=>f.status==='outstanding'),past=r.freight.filter(f=>f.status!=='outstanding');$('#freightSummary').innerHTML=`<strong>${active.length}</strong> outstanding${r.overdueCount?` • <span>${r.overdueCount} overdue</span>`:''}`;list.innerHTML=active.length?'':'<div class="card">No outstanding freight items.</div>';history.innerHTML=past.length?'':'<div class="card">No completed freight history.</div>';active.forEach(f=>appendFreightCard(list,f,true));past.forEach(f=>appendFreightCard(history,f,false));$('#freightHistoryCount').textContent=`(${past.length})`}catch(e){list.innerHTML=`<div class="card error">${esc(e.message)}</div>`}}
function appendFreightCard(el,f,active){const d=document.createElement('article');d.className=`freightCard${f.overdue?' overdue':''}`;const stage={packed:'Packed — awaiting pickup',not_packed:'Not yet packed',pickup_scheduled:'Pickup scheduled',shipped:'Shipped'}[f.stage]||f.stage;d.innerHTML=`<div class="freightTop"><div class="freightLocation">${esc(f.location)}</div><span class="freightStatus">${esc(stage)}</span></div><div class="freightBody">${f.image?`<img src="${attr(f.image)}" alt="">`:''}<div><div class="freightSku">${formatSku(f.sku)}</div><h3>${esc(f.title)}</h3><p>Qty ${esc(f.quantity)} • ${esc(f.batchName||'Pick batch')}</p><p>${(f.orders||[]).map(o=>`Order ${esc(o.orderNumber)}`).join(' • ')}</p>${f.notes?`<p class="freightNote">${esc(f.notes)}</p>`:''}<p class="freightDue">${active?`Follow up: ${fmtDate(f.followUpAt)}`:`${f.status==='completed'?'Completed':'Cancelled'}: ${fmtDate(f.completedAt||f.cancelledAt||f.updatedAt)}`}</p></div></div>${active?`<div class="freightActions">${f.stage==='not_packed'?'<button data-action="mark_packed" class="primary">Mark Packed</button>':''}<button data-action="pickup_scheduled">Pickup Scheduled</button><button data-action="complete" class="completeFreight">Mark Shipped / Complete</button><button data-action="return_to_pick">Return to Pick List</button><button data-action="cancel" class="danger">Cancel Tracking</button></div>`:''}`;if(active)d.querySelectorAll('[data-action]').forEach(btn=>btn.onclick=()=>freightAction(f,btn.dataset.action,btn));el.appendChild(d)}
async function freightAction(f,action,btn){let followUpAt;if(action==='complete'&&!confirm(`Mark ${f.sku} as shipped and complete its freight tracking?`))return;if(action==='return_to_pick'&&!confirm('Return this item to its original pick batch? It will need to be picked there.'))return;if(action==='cancel'&&!confirm('Cancel freight tracking for this item?'))return;if(action==='pickup_scheduled'){const when=prompt('Enter the pickup follow-up date and time:',new Date(Date.now()+86400000).toLocaleString());if(when===null)return;const parsed=new Date(when);if(!Number.isFinite(parsed.getTime())){alert('Enter a valid date and time.');return}followUpAt=parsed.toISOString()}const old=btn.textContent;btn.disabled=true;btn.textContent='Saving…';try{await api('/api/freight/'+f.id,{method:'PATCH',body:JSON.stringify({action,followUpAt})});await loadFreightQueue()}catch(e){alert(e.message);btn.disabled=false;btn.textContent=old}}
function appendDeletedCard(el,b){const d=document.createElement('div');d.className='batch deletedBatch';d.innerHTML=`<div><h3>${esc(b.name)}</h3><div class="meta">${fmtDate(b.createdAt)} • ${b.orderCount} orders • ${b.uniqueStops} pick stops • ${b.totalUnits} units</div></div><button class="restoreBtn">Restore</button>`;d.querySelector('.restoreBtn').onclick=async e=>{e.stopPropagation();if(!confirm('Restore this deleted pick batch?'))return;try{await api('/api/batches/'+b.id+'/restore',{method:'POST',body:'{}'});await loadBatches();await loadPreview()}catch(err){alert(err.message)}};el.appendChild(d)}
$('#createBtn').onclick=async()=>{if(!confirm('Create a frozen snapshot of all current qualifying orders? Orders that arrive later will NOT be added to this batch.'))return;const b=$('#createBtn');b.disabled=true;b.textContent='Creating snapshot…';try{const r=await api('/api/batches',{method:'POST',body:'{}'});await openBatch(r.batch.id)}catch(e){$('#homeError').textContent=e.message}finally{b.disabled=false;b.textContent='Create Pick Batch'}};
async function openBatch(id){const r=await api('/api/batches/'+id);currentBatch=r.batch;currentLines=r.lines;idx=Math.min(currentBatch.currentIndex||0,Math.max(0,currentLines.length-1));renderBatch();show('batchView')}
function renderBatch(){const b=currentBatch;$('#batchHead').innerHTML=`<h2>${esc(b.name)}</h2><p>${fmtDate(b.createdAt)} • <strong>${b.orderCount}</strong> orders • <strong>${b.uniqueStops}</strong> pick stops • <strong>${b.totalUnits}</strong> total units</p><p>${b.pickedStops}/${b.uniqueStops} pick stops completed</p>`;$('#lineTableWrap').innerHTML=`<div class="printTitle hidden"><h1>Pick List — ${esc(b.name)}</h1><p>${fmtDate(b.createdAt)} • ${b.orderCount} orders • ${b.totalUnits} units</p></div><table><thead><tr><th>Location</th><th>Qty</th><th>Image</th><th>SKU</th><th>Title</th><th>Condition</th><th>On Hand</th><th>Orders</th></tr></thead><tbody>${currentLines.map(l=>`<tr><td><strong>${esc(l.location)}</strong></td><td><strong>${l.quantityToPick}</strong></td><td>${l.image?`<img src="${attr(l.image)}">`:''}</td><td>${l.sellerChampProductUrl?`<a class="skuProductLink" href="${attr(l.sellerChampProductUrl)}" target="_blank" rel="noopener noreferrer">${esc(l.sku)}</a>`:esc(l.sku)}</td><td>${esc(l.title)}</td><td>${esc(l.condition)}</td><td>${l.quantityOnHand}</td><td class="ordersSmall">${(l.orders||[]).map(o=>`<div class="orderBreakdownLine">QTY- ${esc(o.quantity)}&nbsp;&nbsp; Order#- ${esc(o.orderNumber)}</div>`).join('')}${b.status==='archived'?`<div class="archiveInventoryControls"><button class="returnInventoryBtn" data-line="${attr(l.id)}" data-picked="${attr(l.quantityToPick)}">Add Back to Inventory</button><button class="setInventoryBtn" data-line="${attr(l.id)}">Update Inventory</button></div>`:''}</td></tr>`).join('')}</tbody></table>`;if(b.status==='archived'){document.querySelectorAll('.returnInventoryBtn').forEach(btn=>btn.onclick=()=>archiveInventoryAction(btn.dataset.line,'return',Number(btn.dataset.picked)));document.querySelectorAll('.setInventoryBtn').forEach(btn=>btn.onclick=()=>archiveInventoryAction(btn.dataset.line,'set'))}}
async function archiveInventoryAction(lineId,mode,pickedQty){
  const line=currentLines.find(x=>x.id===lineId);if(!line)return;let qty;
  if(mode==='return'){
    const raw=prompt(`How many should be added back to inventory at ${line.location}? You picked ${pickedQty}.`,String(pickedQty));if(raw===null)return;qty=Number(raw);
    if(!Number.isInteger(qty)||qty<1||qty>pickedQty){alert(`Enter a whole number from 1 to ${pickedQty}.`);return}
    if(!confirm(`Add ${qty} back to SellerChamp inventory for ${line.sku} at ${line.location}?`))return;
  }else{
    const raw=prompt(`Set the current SellerChamp quantity for ${line.sku} at ${line.location}:`,String(line.quantityOnHand));if(raw===null)return;qty=Number(raw);
    if(!Number.isInteger(qty)||qty<0){alert('Enter a whole-number quantity of 0 or greater.');return}
    if(!confirm(`Set SellerChamp inventory for ${line.sku} at ${line.location} to ${qty}?`))return;
  }
  try{const r=await api(`/api/batches/${currentBatch.id}/lines/${lineId}/archive-inventory`,{method:'POST',body:JSON.stringify({mode,quantity:qty})});const ix=currentLines.findIndex(x=>x.id===lineId);if(ix>=0)currentLines[ix]=r.line;alert(r.message);renderBatch()}catch(e){alert(e.message)}
}
$('#printBtn').onclick=()=>window.print(); $('#startBtn').onclick=()=>{show('pickerView');renderPick()};
$('#deleteBtn').onclick=async()=>{if(!confirm('Delete this pick batch? Its orders will become eligible for a future batch again.'))return;try{await api('/api/batches/'+currentBatch.id,{method:'DELETE'});loadHome()}catch(e){alert(e.message)}};
function formatSku(sku){const s=String(sku??'');if(s.length<5)return esc(s);const a=esc(s.slice(0,4)),b=esc(s.slice(4,10)),c=esc(s.slice(10));return `${a}<span class="skuFocus">${b}</span>${c}`}
function renderPick(){if(!currentLines.length)return;const l=currentLines[idx];$('#progressText').textContent=`Pick stop ${idx+1} of ${currentLines.length} • ${currentLines.filter(x=>x.picked).length} complete`;$('#progressBar').style.width=((idx+1)/currentLines.length*100)+'%';$('#pickLocation').textContent=l.location;$('#pickQty').textContent=l.quantityToPick;$('#pickSku').innerHTML=formatSku(l.sku);$('#pickCondition').textContent=l.condition;$('#pickOnHand').textContent=l.quantityOnHand;$('#pickTitle').textContent=l.title;$('#orderBox').innerHTML=(l.orders||[]).map(o=>`<div class="orderBreakdownLine">QTY- ${esc(o.quantity)}&nbsp;&nbsp; Order#- ${esc(o.orderNumber)}</div>`).join('');const img=$('#pickImage'),no=$('#noImage');if(l.image){img.src=l.image;img.classList.remove('hidden');no.classList.add('hidden')}else{img.classList.add('hidden');no.classList.remove('hidden')};const multi=l.quantityToPick>1;$('#multiConfirm').classList.toggle('hidden',!multi);$('#confirmQty').checked=!!l.picked;const verifyOnHand=Number(l.quantityOnHand)>=1&&Number(l.quantityOnHand)<=3;$('#onHandVerifyWrap').classList.toggle('hidden',!verifyOnHand);$('#confirmOnHand').checked=verifyOnHand?!!l.onHandVerified:true;$('#confirmLabel').textContent=`I picked all ${l.quantityToPick}`;$('#onHandLabel').textContent='VERIFIED';$('#correctionPanel').classList.add('hidden');$('#actualQty').value='';$('#correctionMsg').textContent='';$('#correctQtyBtn').textContent='Adjust Quantity';$('#pickError').classList.add('hidden');$('#pickError').textContent='';$('#backBtn').disabled=idx===0;$('#nextBtn').textContent=idx===currentLines.length-1?'Finish':'Next →';const pickerSc=$('#pickerSellerChampBtn');if(l.sellerChampProductUrl){pickerSc.href=l.sellerChampProductUrl;pickerSc.classList.remove('hidden')}else{pickerSc.removeAttribute('href');pickerSc.classList.add('hidden')}const ebay=$('#ebayListingBtn'),sc=$('#sellerChampProductBtn');if(l.ebayListingUrl){ebay.href=l.ebayListingUrl;ebay.classList.remove('hidden')}else{ebay.removeAttribute('href');ebay.classList.add('hidden')}if(l.sellerChampProductUrl){sc.href=l.sellerChampProductUrl;sc.classList.remove('hidden')}else{sc.removeAttribute('href');sc.classList.add('hidden')}}
$('#backBtn').onclick=async()=>{if(idx>0){idx--;await saveIndex();renderPick();requestAnimationFrame(()=>window.scrollTo({top:0,left:0,behavior:'auto'}))}};
$('#nextBtn').onclick=async()=>{const l=currentLines[idx];const verifyOnHand=Number(l.quantityOnHand)>=1&&Number(l.quantityOnHand)<=3;if(verifyOnHand&&!$('#confirmOnHand').checked){$('#pickError').textContent='Did you verify the quantity on hand?';$('#pickError').classList.remove('hidden');return}if(l.quantityToPick>1&&!$('#confirmQty').checked){$('#pickError').textContent='Did you pick the full quantity?';$('#pickError').classList.remove('hidden');return}try{await api(`/api/batches/${currentBatch.id}/lines/${l.id}`,{method:'PATCH',body:JSON.stringify({picked:true,onHandVerified:verifyOnHand?true:!!l.onHandVerified})});l.picked=true;if(verifyOnHand)l.onHandVerified=true;if(idx===currentLines.length-1){await api('/api/batches/'+currentBatch.id,{method:'PATCH',body:JSON.stringify({status:'archived',currentIndex:idx})});alert('Pick batch complete and archived.');await loadHome();return}idx++;await saveIndex();renderPick();requestAnimationFrame(()=>window.scrollTo({top:0,left:0,behavior:'auto'}))}catch(e){alert(e.message)}};
$('#correctQtyBtn').onclick=()=>{const p=$('#correctionPanel');const opening=p.classList.contains('hidden');p.classList.toggle('hidden');$('#correctQtyBtn').textContent=opening?'Close Adjustment':'Adjust Quantity';if(opening){$('#actualQty').value=currentLines[idx].quantityOnHand;$('#actualQty').focus()}else{$('#correctionMsg').textContent=''}};
$('#updateQtyBtn').onclick=async()=>{const l=currentLines[idx];const q=Number($('#actualQty').value);if(!Number.isInteger(q)||q<0){$('#correctionMsg').textContent='Enter a whole-number quantity of 0 or greater.';return}if(!confirm(`Update SellerChamp quantity at ${l.location} from ${l.quantityOnHand} to ${q}?`))return;const btn=$('#updateQtyBtn');btn.disabled=true;btn.textContent='Updating…';$('#correctionMsg').textContent='';try{const r=await api(`/api/batches/${currentBatch.id}/lines/${l.id}/correct-inventory`,{method:'POST',body:JSON.stringify({quantity:q})});Object.assign(l,r.line);$('#pickOnHand').textContent=l.quantityOnHand;const verifyOnHand=Number(l.quantityOnHand)>=1&&Number(l.quantityOnHand)<=3;$('#onHandVerifyWrap').classList.toggle('hidden',!verifyOnHand);$('#confirmOnHand').checked=verifyOnHand?true:true;$('#onHandLabel').textContent='VERIFIED';$('#correctionMsg').textContent=`Updated SellerChamp: ${r.message}`;$('#correctionPanel').classList.remove('hidden');$('#pickError').classList.add('hidden')}catch(e){$('#correctionMsg').textContent=e.message}finally{btn.disabled=false;btn.textContent='Update SellerChamp'}};
$('#freightBtn').onclick=()=>{$('#freightPanel').classList.remove('hidden');$('#freightNote').value='';$('#freightError').textContent='';requestAnimationFrame(()=>$('#freightPanel').scrollIntoView({behavior:'smooth',block:'center'}))};
$('#freightCancelBtn').onclick=()=>$('#freightPanel').classList.add('hidden');
$('#freightPackedBtn').onclick=()=>markCurrentFreight('packed',$('#freightPackedBtn'));
$('#freightNotPackedBtn').onclick=()=>markCurrentFreight('not_packed',$('#freightNotPackedBtn'));
async function markCurrentFreight(stage,btn){const l=currentLines[idx];if(!l||l.freightId)return;const label=stage==='packed'?'packed and awaiting pickup':'not yet packed';if(!confirm(`Move ${l.sku} to Outstanding Freight as ${label}?`))return;const old=btn.textContent;btn.disabled=true;btn.textContent='Saving…';$('#freightError').textContent='';try{const r=await api(`/api/batches/${currentBatch.id}/lines/${l.id}/freight`,{method:'POST',body:JSON.stringify({stage,notes:$('#freightNote').value})});Object.assign(l,r.line);currentBatch=r.batch;$('#freightPanel').classList.add('hidden');const next=currentLines.findIndex((x,i)=>i>idx&&!x.picked&&!x.freightId);if(next>=0){idx=next;await saveIndex();renderPick();window.scrollTo({top:0,left:0,behavior:'auto'})}else{await api('/api/batches/'+currentBatch.id,{method:'PATCH',body:JSON.stringify({status:'archived',currentIndex:idx})});alert('Freight item saved. This pick batch is complete and archived.');await loadHome()}}catch(e){$('#freightError').textContent=e.message}finally{btn.disabled=false;btn.textContent=old}}
$('#confirmQty').onchange=()=>$('#pickError').classList.add('hidden');
$('#confirmOnHand').onchange=()=>$('#pickError').classList.add('hidden');
async function saveIndex(){await api('/api/batches/'+currentBatch.id,{method:'PATCH',body:JSON.stringify({currentIndex:idx,status:'in_progress'})})}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))} function attr(s){return esc(s)}
loadHome().catch(()=>{});
